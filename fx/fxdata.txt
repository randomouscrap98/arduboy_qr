// Need at least a small amount of data in the fx
string whatever = "this is silly"

savesection

// I mostly just want to read save raw, so this is here just for fun
raw_t savbuf = "save_init.bin"
